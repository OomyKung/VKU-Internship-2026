Dataset: graph_spa_500_0
Method: vgae
Family: Autoencoder / generative GNN

What this method does:
VGAE encodes nodes with a GNN into a latent distribution and learns embeddings by reconstructing graph connectivity from that latent space.

Typical strengths:
Combines topology with node features and encourages a smooth latent space.

Typical weaknesses:
The reconstruction objective can be less directly aligned with class separation than contrastive methods.

Observed benchmark result:
status=ok, runtime=16.147s, embedding_dim=64, label_probe_accuracy=0.8400, uses_features=True

Task-level evaluation:
- node_classification: status=ok, label_column=age, runtime=0.020s, accuracy=0.8400, macro_f1=0.8044, micro_f1=0.8400, roc_auc=-, average_precision=-, nmi=-, ari=-
  notes: classes=7; stratified=True

Why this result likely happened:
VGAE scored strongly because it combines graph reconstruction with feature-aware latent representations, but its objective is slightly less directly aligned with classification than the strongest contrastive methods. A major caveat: this benchmark's feature preparation includes node attributes, and the evaluation target 'age' is one of those attributes on this dataset. That likely makes the result substantially easier for feature-based methods.

Caveat:
This explanation is an inference from the saved benchmark outputs and the current code path. It is a strong engineering interpretation, not a formal causal proof.
