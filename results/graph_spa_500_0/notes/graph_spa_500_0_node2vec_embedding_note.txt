Dataset: graph_spa_500_0
Method: node2vec
Family: Shallow biased random-walk

What this method does:
Node2Vec extends DeepWalk by biasing the walks with p/q parameters so it can favor BFS-like local exploration or DFS-like outward exploration.

Typical strengths:
Can balance community-level similarity and structural-role similarity.

Typical weaknesses:
Still structure-only here, so attribute-driven class signals are not used directly.

Observed benchmark result:
status=ok, runtime=7.985s, embedding_dim=64, label_probe_accuracy=0.7280, uses_features=False

Task-level evaluation:
- node_classification: status=ok, label_column=age, runtime=0.017s, accuracy=0.7280, macro_f1=0.6799, micro_f1=0.7280, roc_auc=-, average_precision=-, nmi=-, ari=-
  notes: classes=7; stratified=True

Why this result likely happened:
node2vec performed in the middle because random-walk co-occurrence captures higher-order topology better than LINE, but it still does not use node attributes directly. In this run it ended up effectively matching DeepWalk, which suggests the chosen p/q bias did not add useful extra signal.

Caveat:
This explanation is an inference from the saved benchmark outputs and the current code path. It is a strong engineering interpretation, not a formal causal proof.
