Dataset: graph_spa_500_0
Method: line
Family: Shallow proximity-preserving

What this method does:
LINE preserves first-order proximity between linked nodes and second-order proximity between nodes with similar neighborhoods. It is fast and focused on local graph structure.

Typical strengths:
Very fast and simple, especially on sparse static graphs.

Typical weaknesses:
Local-only objective usually misses longer-range community structure and attribute information.

Observed benchmark result:
status=ok, runtime=0.174s, embedding_dim=64, label_probe_accuracy=0.6160, uses_features=False

Task-level evaluation:
- node_classification: status=ok, label_column=age, runtime=0.022s, accuracy=0.6160, macro_f1=0.5283, micro_f1=0.6160, roc_auc=-, average_precision=-, nmi=-, ari=-
  notes: classes=7; stratified=True

Why this result likely happened:
LINE was the weakest method here because it only optimizes local proximity. That is usually not enough for a 5-class task like 'age', where broader community structure and rich attributes matter.

Caveat:
This explanation is an inference from the saved benchmark outputs and the current code path. It is a strong engineering interpretation, not a formal causal proof.
