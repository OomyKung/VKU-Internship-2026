Dataset: graph_spa_500_0
Method: gcn
Family: Message-passing GNN

What this method does:
GCN repeatedly averages transformed neighbor features, so each node embedding becomes a smoothed summary of its local neighborhood.

Typical strengths:
Strong baseline when labels correlate with local homophily and useful node features exist.

Typical weaknesses:
Can over-smooth if neighborhoods are mixed or if too much averaging washes out boundaries.

Observed benchmark result:
status=ok, runtime=15.179s, embedding_dim=64, label_probe_accuracy=0.8480, uses_features=True

Task-level evaluation:
- node_classification: status=ok, label_column=age, runtime=0.024s, accuracy=0.8480, macro_f1=0.8253, micro_f1=0.8480, roc_auc=-, average_precision=-, nmi=-, ari=-
  notes: classes=7; stratified=True

Why this result likely happened:
GCN scored strongly because it propagates feature information through local neighborhoods, which is effective on homophilous graphs. A major caveat: this benchmark's feature preparation includes node attributes, and the evaluation target 'age' is one of those attributes on this dataset. That likely makes the result substantially easier for feature-based methods.

Caveat:
This explanation is an inference from the saved benchmark outputs and the current code path. It is a strong engineering interpretation, not a formal causal proof.
